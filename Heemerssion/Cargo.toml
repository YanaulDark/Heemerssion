[package]
name = "Heemerssion"
version = "0.1.0"
edition = "2021"  # edition 2024 пока не стабильна, лучше использовать 2021

[dependencies]
libc = "0.2"

[build-dependencies]
cc = "1.0"
bindgen = "0.69"  # Исправлено: bindgen, а не bingden